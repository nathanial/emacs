[package]
name = "hexl"
version = "0.1.0"
edition.workspace = true
publish = false
license = "GPL-3.0-or-later"

[dependencies]
anyhow = { version = "1", default-features = false, features = ["std"] }
